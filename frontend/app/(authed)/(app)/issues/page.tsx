import { IssuesRouteContent } from '@/components/site-health/site-health-route-content';

/** Next entry for the shared Issues catalog route content. */
export default function IssuesPage() {
  return <IssuesRouteContent />;
}
