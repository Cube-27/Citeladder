import { AcceptInvitationRouteContent } from '@/components/settings/settings-route-content';

/** Next entry for the shared workspace-only invitation acceptance route. */
export default function AcceptInvitationPage() {
  return <AcceptInvitationRouteContent />;
}
