import { SettingsRouteContent } from '@/components/settings/settings-route-content';

/** Next entry for the shared settings route content. */
export default function SettingsPage() {
  return <SettingsRouteContent />;
}
