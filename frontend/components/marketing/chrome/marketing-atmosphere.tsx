/**
 * The public surface's ambient field — two slow accent auras that drift behind
 * every marketing page. Purely decorative (aria-hidden), styled and animated
 * entirely from globals.css (`.marketing-atmosphere`), and frozen under
 * reduced motion.
 */
export function MarketingAtmosphere() {
  return <div aria-hidden className="marketing-atmosphere" />;
}
