# Retired AWS starter

The former multi-service ECS starter was removed because it conflicts with the
one-task, seven-day demo deployment. The active AWS implementation is
[`../terraform/README.md`](../terraform/README.md).

Do not restore or run the old `deploy-ecs.ps1` path for `citeladder.com`.
