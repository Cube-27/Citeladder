# CiteLadder billing, credits and BYOK consolidation

> Approved by the user on 2026-09-07 in Vorflux plan #1160 (version 2).
> This is approved future work, not a claim of implementation. Follow the progress record.
> Historical “proposed/approval” wording below records the reviewed plan; its stated defaults are approved, while explicitly unset commercial values and live actions remain unapproved.


### Summary
**Proposed approval plan:** all three read-only audits and the user's final decisions are incorporated, including genuine Content/Growth Agent BYOK and the temporary no-card offer for **any new account**. No company-email or email-verification requirement applies to launch; no mail service is added. Proposed policy defaults below are part of plan approval, not already-shipped behavior. Unset rates/quantities remain unavailable, never guessed.

Consolidate CiteLadder billing around the existing account, workspace links, subscriptions, append-only grants/revocations and consumable ledger. Move commercial policy into validated database configuration, close accounting and authorization gaps, and make the existing pricing and Settings UI show the same server-owned terms and effective access. Implement in bounded phases only after this plan is approved and a documentation-only checkpoint is pushed to existing PR [#35](https://github.com/Cube-27/Citeladder/pull/35).

### Approval boundary and durable checkpoint
- Audited checkout: `/code/Cube-27/Citeladder`, local `vorflux/billing-consolidation`, HEAD `3bc20cf761bb489da77ddd35b1d6c1bcc5737782`. PR #35 uses remote head `fix/live-project-controls-and-frontend-review`; baseline main is `52c4a65ae97851812a3e09d430b8ff541940c589`.
- This is a proposed plan, not permission to implement. Before approval: no repository implementation, schema changes, reset, provider calls, or code push. Plan drafts and read-only audit/design work are outside the repository.
- After explicit approval, first commit **only** the approved plan to `docs/plans/billing-consolidation.md`, summary to `docs/plans/billing-consolidation-summary.md`, evidence to `docs/audits/billing-consolidation-2026-09-07.md`, and progress to `docs/plans/billing-consolidation-progress.md`. Preserve the approved design manifest/mockups under `docs/plans/billing-consolidation-designs/` and link the plan in `docs/README.md`. Record actual approval/date and unresolved decisions; do not invent approval.
- Fetch the PR head, verify PR #35 still targets the expected repository/branch, and preserve all its unrelated work. The local branch name does not authorize a new PR. Push the documentation checkpoint with the equivalent of `git push origin HEAD:fix/live-project-controls-and-frontend-review`, then verify PR #35 shows that commit **before any implementation**. If remote work advanced or histories diverge, reconcile it without force-pushing or replacing the PR. Never create another PR.
- **Documentation-only checkpoint gate:** review the approved text, relative links/design artifacts, exact PR target and `git diff --check`; verify the commit contains documentation only. This is not an implementation-complete claim. No application dependency installation, backend test suite or full static/browser harness is required before that checkpoint. The full implementation gate below applies before declaring application work complete or pushing implementation, not before preserving the approved plan.
- Resume from the tracked progress file: checkpoint commit, approved policy choices, phase status, changed owners, test commands/results and CI URLs, pending dependencies/provider/legal decisions, and the exact next bounded task. Update it at phase boundaries; never mark a skipped or unavailable check passed.
- The database is disposable until payment is implemented. Fold all schema changes into **only** `migrations/versions/0001_initial.py`; no `0002+`, legacy compatibility, data-preservation backfill, or parallel billing system. Reset/rebuild permission covers disposable local/test databases only—not live, shared, staging or production reset, deployment, or payment activation.

### Audit and scope
**Reuse, do not replace:** `backend/app/models/billing.py` already owns `BillingAccount`, `WorkspaceBillingLink`, `BillingSubscription`, `AccountGrant`, `GrantRevocation`, `ConsumableLedger`, `PendingActivation`, `IdempotencyRecord`, and `BillingWebhookEvent`. `domain/billing/*`, `domain/entitlements/*` and `connectors/billing/*` already provide account authorization, quote/activation boundaries, provider DTOs, grant resolution and reservation/allocation. Current catalog types are in `core/config/billing_catalog.py`.

| Confirmed current behavior | Consequence | Owning phase |
|---|---|---|
| Public signup now grants 1 project / 10 prompts / 20 monitored URLs; development login gains broad grants; paid counters SUM with baseline grants | Free, development and paid access are not cleanly separated; fixture assumptions also changed | 0, 2 |
| Code/env commercial settings and a second frontend price map coexist | Public display, server quote and provider charge can disagree | 1, 3, 5 |
| Funded quote includes base + credit + tax, while subscription creation uses a base-plan reference | Enabling configuration alone can undercharge or grant the wrong bundle | 3 |
| Webhook dedupe is committed before dispatch; reconciliation releases row locks before network I/O without a durable lease | A failed event may never retry; overlapping sweepers can claim the same work | 3 |
| Renewal grant identity uses the current catalog revision; provider active status authorizes grants | A revision change can duplicate period grants; unpaid invoice periods can be funded | 3 |
| One-time creation uses Payment Links while captured-payment handling uses payment IDs | Top-ups need explicit link/order/payment correlation | 3 |
| No-grant and BYOK paths have authorization gaps; frontend access also relies on incomplete heuristics | Owning capabilities must gate API, queue admission, execution and UI independently of checkout | 2, 5 |
| Existing ledger reserves/debits/releases audit credits, but lacks a complete refund and replay-conflict contract | Extend the current ledger, not a second wallet or mutable balance | 2 |

Detailed evidence is in `../audits/billing-consolidation-2026-09-07.md#billing-domain-audit` (F01–F20), `../audits/billing-consolidation-2026-09-07.md#ci-failure-audit` and `../audits/billing-consolidation-2026-09-07.md#content-and-byok-audit` (ten additional evaluated hypotheses). Incorporate all three into the durable PR audit checkpoint. The full finding-to-task map is:

| Audit findings | Bounded correction |
|---|---|
| F01–F03, F18: email privilege, stacked baseline, missing tier flags, recycled dev grant identity | Phase 1–2 explicit DB profile/operation identity, exclusive base replacement, approved flags; never fix by weakening enforcement |
| F04–F07: receipt loss, mutable renewal terms, charge mismatch, link/payment mismatch | Phase 3 durable event recovery, immutable period receipts/terms and exact funded amount/identity parity |
| F08–F10: three conflicting usage calculations, no-grant bypass, per-workspace URL multiplication | Phase 2 one active per-grant ledger projection, fail-closed access, sponsor-account-wide occupancy |
| F11–F13: stale expiry, ambiguous/incomplete provider state, non-durable recovery | Phase 2–3 worker-time checks, finite paid periods, authoritative conflict resolution, durable leases/backoff |
| F14: frontend amounts/currency and wrong account context | Phase 5 remove numeric overlay; workspace-effective capability context separate from private owner finance context |
| F15–F16: display-only capabilities, absent operator policy, flags mistaken for eligibility | Phase 1–4 explicit supported benefit matrix, auditable CLI, verified disabled-by-default routes |
| F17: cache leaks uncommitted/future/identity-map state | Phase 2 remove unsafe cache initially; only committed, fresh effective authorization may be used |
| F19–F20: remaining lock/replay/release gaps, changing allocation order, missing refund | Phase 2–3 shared lock order, full effect fingerprint, frozen allocations and bounded compensations; preserve existing uniqueness safeguards |

**Proven CI baseline:** [run 34132365978](https://github.com/Cube-27/Citeladder/actions/runs/34132365978) at this head has **37 backend failures (3231 passed, 1 skipped)** and **4 E2E failures (32 passed)**; frontend quality/tests/build passed. Previous PR [34129342135](https://github.com/Cube-27/Citeladder/actions/runs/34129342135) and main [34080825458](https://github.com/Cube-27/Citeladder/actions/runs/34080825458) were green. Security/contract were skipped; [Compose 34132366024](https://github.com/Cube-27/Citeladder/actions/runs/34132366024) was green with the actual smoke job **skipped**. Most failures are explicit fixture/contract drift, not proof of broken locks; the authorship vendor-credit false positive is a real independently reproduced source defect. Local classifier tests passed 6/6 and bounded source probes reproduced listed cases; no local pytest, browser, migration or full gate passed.

**Content/Growth Agent scope evidence:** the follow-up audit now covers 17 evaluated hypotheses. The existing `DiscoveryModelCard` is localStorage-only and misleadingly claims workspace persistence; custom model/key/URL execution is not wired. Existing provider routes are fixed measurement routes, and their general URL syntax check is not safe for arbitrary authenticated endpoints. Reuse encrypted connection custody and low-level DNS/IP pinning primitives—not the crawler's credential-free redirecting GET interface. No password email-verification or outbound mail owner exists; neither is added for the final open-to-new-accounts promotion policy.

Neither Content nor Growth Agent uses the commercial ledger today; feature flags and operational job limits do not cap token spend. Content's `ContentGenerationAttempt` exists but loses usage on empty output and has no durable pre-I/O call receipt; Agent has successful-run usage and evidence-tool attempts but no narration-call history. Before this change both use platform credentials independently of audit BYOK; that funding must not be hidden by a BYOK plan label. The approved implementation routes genuine customer-key calls without platform-credit spending and meters only explicitly permitted platform calls. Current Agent tools only read persisted evidence and each attempt narrates at most once; Content and Agent are buffered/non-streaming. Implement leaf-call charging only—no Agent→Content budget-transfer, streaming or nested execution framework. Preserve the future invariant that a supported paid child call needs budget before I/O and must never be charged twice, without implementing absent workflows.

These are source findings, not claims of a live payment incident. No live/sandbox merchant capability has been verified. The current checkout has **no `UsageEvent` model or persisted payment receipt model**: its usage evidence is `ProviderAttempt` / `ExecutionCostProjection` in `models/audit.py`, plus `ConsumableLedger`. Reuse these actual owners and the existing `ProviderPayment` DTO; do not introduce a duplicate `UsageEvent` store merely to match terminology.

**In scope:** three existing tier keys plus contact-only Enterprise; internal free/trial/development/complimentary/paid states; configurable offers, codes and BYOK overrides; credit lifecycle; failed-payment recovery; upgrades/downgrades, recurring add-ons and top-ups; minimal UI updates and auditable operator CLI.

**Not in scope:** tier renaming, sponsor/ownership transfer, dispute console, large admin UI, automatic invoicing/tax/FX engine, general promotions/rules engine, Redis/new queue, unrelated product rewrites, production migration/backfill compatibility, new billing provider, launch/deployment or live payments. Financial evidence only supports the named payment/refund/reconciliation effects; no speculative provider lookup endpoints or undocumented idempotency. **Newly in scope:** per-tier usage credits for token-consuming Content and Growth Agent, not just existing funded audit attempts. Reuse canonical account/grant/ledger/idempotency primitives, Content's existing attempt evidence and one genuinely missing Agent model-attempt owner. **Approved:** one shared `ai_credits` pool for Content + Growth Agent, billed from actual input/output token usage using frozen versioned model rates. Audit credits remain a separate existing unit; no conversion between audit and AI balances is implied. Rate coefficients and per-tier launch quantities remain unset—do not invent values. **Also in scope:** genuine customer model/API-key/base-URL routing for Content and Growth Agent, reusing the audited existing credential UI/API where possible. Customer-key calls spend no platform AI credits and require no additional BYOK credit purchase; feature/occupancy/rate limits still apply. Current code uses platform keys even on BYOK plans, so existing behavior must not be mislabeled BYOK while routing is being fixed. No silent platform fallback.

### Commercial seeds and decisions
**User decision received during planning:** seed the public website's monthly USD amounts—BYOK **$49/$99/$149**, funded **$99/$149/$299**—and **Tier 1-only** trial. These prices and trial tier are resolved; do not ask again. The table records the current disagreement being removed. Funded checkout remains unavailable until explicit included allowances and provider readiness are configured and verified; approving amounts does not activate payments or approve tax treatment.

| Existing key | Public UI BYOK / funded per month | Backend BYOK base per month | Backend funded |
|---|---:|---:|---|
| `tier_1` | $49 / $99 | $99 | Base + derived credit price; margin unset, so unavailable |
| `tier_2` | $99 / $149 | $199 | Same unavailable derivation |
| `tier_3` | $149 / $299 | $299 | Same unavailable derivation |
| `enterprise` | Contact sales | Contact sales | No self-serve price/grants |

Evidence: `frontend/lib/config/billing.ts:25–37`, `backend/app/core/config/billing_contracts.py:155–159`, `billing_settings.py`, `billing_catalog.py`. India presently depends on an unset USD/INR rate; 18% GST is a code default, not verified tax/legal advice.

| Policy | Proposed seed for explicit approval | Remains unavailable / never inferred |
|---|---|---|
| Tier structure and non-price allowances | Keep `tier_1/2/3/enterprise`; **approved: Content + Growth Agent enabled on Tier 2/3 only**, not Tier 1; explicit operator override remains separate. Preserve current other limits as draft DB seed (projects 1/3/10, prompts 10/30/60, monitored URLs 50/150/400) | Feature permission is distinct from sufficient token-consuming usage credits and provider credentials. Shared `ai_credits` and token-weighted versioned rates are approved; coefficients/finite platform allowances remain unset. True BYOK bypasses platform-credit spending, not product permissions |
| Prices, funded budget and included credits | **Approved USD/month:** BYOK 4900/9900/14900; funded 9900/14900/29900 minor units. Store funded variant as that exact approved sell price, not the current $500 budget-plus-margin formula | Included credits and trial funded allowance stay unset until explicitly configured; funded purchase/admission remains unavailable. No guessed FX, tax, margin, pack size or provider mapping. Keep operational cost protection separate from commercial units |
| Free baseline | Existing 1 project / 10 prompts / 20 monitored URLs; no funded credits; no Content creation/Growth Agent grant | Free baseline is not a trial/promotion and never stacks with a higher primary bundle |
| **Approved no-card promotion** | **Phase 1 seeds a draft/disabled no-card campaign. Enable only through an audited operator action after the Phase 4 claim, eligibility, lifetime-intro and idempotency acceptance gates pass**, then offer exclusive **seven-day Tier 1 to any eligible new-registration account only after its explicit self-serve claim**. Cohort starts at that audited enablement, not Phase 1 seed/publication; do not invent a campaign end date. This sequence does not authorize live enablement or deployment. **No company/email verification gate, automatic claim, card/provider dependency, renewal mandate or charge**. Paid checkout remains off | Proposed: one introductory benefit per account lifetime across this promotion and the future card trial. Later optional OAuth-verified work-email policy uses existing trusted identity evidence and DB domain rules; it adds no mail service and does not claim verified employment. Operator exceptions are bounded/audited; manual complimentary access is a separate explicit action |
| Development / complimentary | Operator-selected explicit grant bundle, actor/reason, start/end, and optional BYOK permission; development provisioning restricted to development/disposable targets | No production email-based privilege bypass; no fake paid provider subscription; no implicit funded spend or unlimited money |
| Trial | **Approved: Tier 1 only, 7 full days**, new account only, at most once after verified hosted **credit/debit card** recurring authorization and explicit consent. Clearly show Tier 1 renewal price/mode; another tier requires explicit selection | No access from signup, redirect, key presence, generic status or UPI/eMandate. No Tier 2/3/Enterprise trial. Funded trial units remain unset; no hidden switch of renewal tier/mode |
| Renewal and grace | One frozen recurring bundle per canonical subscription-period, irrespective of receipt count; included credits expire at period end, with **no rollover**. Propose retaining already-issued non-funded access only to its approved end/grace boundary | A failed invoice, pending/halted status or grace never issues fresh credits or permits new platform-funded reservations unless a separate valid explicit grant authorizes them. Grace duration itself remains unapproved |
| Top-up expiry | Proposed retention of current **30 days capped at base-subscription access end**, shown before purchase; earliest effective expiry first | This cap can shorten purchased credit life, so it requires explicit approval. Alternative: fixed 30-day validity with spend suspended when base access is absent. No silent policy choice |
| Plan changes | Recommend cycle-end upgrades and downgrades for the first slice; one pending change, explicit effective date; no destructive deletion when over limit | Immediate upgrades/proration require separately approved calculation, verified method support and paid-delta settlement; no generic proration promise |
| Recurring add-ons | Separate recurring subscriptions, current per-account/per-key slots; align access with valid base access; preserve already-paid evidence | Prices/quantity commercial ceilings and multi-mandate UX need approval; never implement recurring charges with Razorpay's upfront `addons` field |
| Offers/codes | Validated flat/percentage terms, date/region/currency/plan/mode scopes, limits, duration and stable IDs; one offer per purchase, no stacking by default | Seed no active code/discount. Unsupported provider offer/method/currency combinations stay unavailable; internal discounted amount must equal actual charge |
| Credit corrections / money refunds | Refund credits by immutable compensation linked to the original debit; cap at net consumed units and preserve original expiry | No automatic cash refund from a local credit correction; payment refunds need verified provider receipt/operator authority. Expired refunds do not revive spendable credit |
| **Approved BYOK** | Customer keys/models/base URLs cover their LLM calls, including Content/Growth Agent, with explicit plan/override permission. Genuine customer-key calls need/spend **no platform credits**; a BYOK plan does not receive an invented extra-credit pack | Never infer funding from the subscription name: freeze actual credential/route source. Existing platform-key calls remain metered unless replaced by true BYOK. Invalid/missing/revoked customer keys fail closed; no silent platform fallback. Custom endpoints require existing SSRF/network protections and safe credential custody |
| Content/Agent failed or uncertain usage | Proposed: charge confirmed usage even when parsing/output/product result fails; pre-I/O/no-model fallback consumes no model credits. For missing required usage, retain safe evidence and a configurable bounded hold—recommend **24 hours from dispatch**, then release unresolved holds and record platform absorption/operator review | Never fabricate tokens, silently bill a timeout as exact token usage, allow indefinite holds, debit beyond the reserved customer cap, or erase a real expense. Customer-favoring absorption/compensation does not imply zero provider money; these policy proposals require plan approval |

**Policy proposals included in plan approval:** retain the disclosed 30-day/base-end top-up cap; **AUDIT credits only** remain one provider-dispatched execution attempt per credit, including retries/timeouts. The separate Content/Growth `ai_credits` pool uses actual token-weighted versioned rates, never attempt-count charging. Release unused reservations, permit no included-credit rollover, and apply next-cycle plan/add-on quantity changes without proration. Require explicit finite credit quantities; only occupancy/rate policy may represent unlimited. Approval records acceptance or changes to these proposals; engineers must not pick different terms later.

**Final user decisions:** Tier 2/3 Content + Growth Agent; shared token-weighted/versioned-rate `ai_credits` for platform calls; true customer-key calls spend none; temporary no-card Tier 1 access is open to any new account with **no launch email gate**. **Still unavailable until configured/verified:** actual rate coefficients/finite platform allowances, non-USD prices/taxes, grace and legal/provider readiness. These are configuration/launch prerequisites, not unanswered architecture questions; do not invent them. Missing platform metering values do not prevent an otherwise permitted genuine BYOK call. The verified-card gate applies only to the future recurring-renewal trial.

**Introductory-access proposal for plan approval:** keep distinct promotion/card-trial kinds but **one lifetime introduction slot per billing account**. New means an account first registered in the campaign's eligible cohort, with no previous introduction/paid/development/complimentary access; merely logging in, repairing billing links or creating another workspace is not new registration. An explicit authenticated claim starts the seven-day clock, not registration. A no-card claim consumes future card-trial eligibility; cancellation, refund, catalog revision or workspace creation never resets it. An operator may instead issue a separately audited, time-bounded complimentary grant—never automatically restore the intro slot or schedule payment. This is an account rule, not protection against one person registering different addresses; retain existing signup abuse controls. No sponsor transfer, new verification mail service or cross-person/card tracking.

**Illustrative operator composition—not seed policy:** an authorized operator could issue **30 days of Tier 2 complimentary access + a finite 2,000-AI-credit grant expiring with that access**, and prepare an optional **20%-off offer for the first three paid cycles**. Reuse the same catalog, grant, activation and audit primitives; require actor/reason, explicit target/expiry, idempotency and dry-run. These numbers are examples only: no 2,000-credit or 20% offer is seeded, no payment is scheduled by the complimentary grant, and the discount cannot become purchasable until its exact provider binding and later renewal terms are verified. The Tier 1-only restriction applies to **self-serve introductory offers/trials**, not separately approved operator complimentary tiers. No generic rules engine or admin UI is needed.

### Smallest persisted owner changes
All changes below live in the existing billing and native execution model/domain owners and the single initial migration. Keep code-owned capability vocabulary, validators and executable adapter support in code; keep commercial values and non-secret mappings in the database. Environment/secret manager owns **merchant/platform credentials, the encryption master key**, webhook/quote-signing secrets, transport/lease tuning and emergency switches—not commercial policy. **Delegated decision, selected by the main agent:** the user declined to choose customer-key custody and requested best judgment; reuse existing encrypted, write-only customer BYOK key storage in `ProviderConnection` with the master key outside the database, redacted access/rotation and runtime-only plaintext. This was delegated, not explicitly user-selected. Merchant/platform keys never enter commercial/tenant DB records; no new secret service or per-customer environment variables.

| Persisted owner | Smallest change | Why it is necessary / what it replaces |
|---|---|---|
| **New `BillingCatalogRevision`** | UUID/revision, validated immutable JSON payload reusing catalog types, publication state/active uniqueness, actor/reason/time; payload holds prices/tax treatment, grant templates, access profiles, offers/codes, expiry/renewal rules and private non-secret provider mappings/capability verification | One versioned commercial source instead of environment knobs and frontend amounts; no plan/price/offer/mapping table per concept. Draft validation and publication are transactional; frozen purchases still read their original revision |
| `BillingAccount` + workspace links + `BillingCustomer` | Keep account/sponsor ownership and existing opaque provider-customer mapping; add internal access-profile/intro eligibility projection and one lifetime intro reservation/consumed fact with distinct promotion/card-trial provenance and lifecycle-version invalidation | Distinguishes free, trial, development, complimentary and paid without overloading provider status. Effective permission still comes only from valid grants; do not create a workspace wallet or fake subscription |
| `BillingSubscription` | Freeze purchased catalog revision, credential mode and grant/charge terms; retain provider status separately; reference scheduled change intent/effective period | Normalize authoritative cycle start/end against this internal subscription UUID and enforce owning account. Freeze one bundle of terms for each accepted period using existing subscription/grant provenance; later receipts cannot change it. One current base/per-key recurring add-on invariants remain; no new period table or agreement store |
| `AccountGrant` / `GrantRevocation` | Add primary-bundle versus additive role, explicit unlimited marker only for approved occupancy/rate grants, and complete actor/reason/operation provenance; preserve append-only values, expiry and revocation. Encode canonical recurring subscription-period-purpose identity in existing source keys and enforce DB uniqueness | Recurring identity is normalized `(internal subscription UUID, authoritative cycle start, authoritative cycle end, stable grant purpose)`, with account ownership enforced—not receipt, invoice, quote or catalog revision. Preserve the whole period bundle's frozen terms/provenance. Top-ups instead use canonical provider/payment identity plus purpose. Select one base then explicit supplements; replacement remains revoke + issue. No new identity table or abstraction |
| `ConsumableLedger` | Keep allocations; make audit/task FKs nullable only together and add `content_generation_id` / `agent_task_run_id` real `RESTRICT` FKs with exact-one-branch CHECK. Add per-call/allocation debit uniqueness, typed attempt references, complete fingerprints, bounded holds and debit-linked refunds | One ledger serves existing audit attempt units and variable-unit Content/Agent charges. Freeze account/workspace/subject/grant/capability ownership, use composite FK checks where practical, and reject arbitrary generic subject UUIDs. One settlement can consume several reserved grants; no single-grant requirement, mutable balance or duplicate wallet |
| `PendingActivation` | Extend kinds to no-card promotion/card trial/plan change; freeze quote or grant terms, consent versions/time/actor, stable offer/code identity and reservation outcome; add durable claim/lease and provider link/invoice/payment correlation references | Reuses commercial intent for quote, once-only trial slots, scheduled changes and coupon reservation/redemption. Count pending + successful redemptions with account/offer locks and DB uniqueness; abandoned/failed claims release capacity, successful ones remain immutable history |
| **New `BillingPayment`** | Normalized append-only settlement/refund receipt, unique provider/payment identity, invoice/period/intent link, amount/currency, paid-at and refund parent | `ProviderPayment` exists only as a DTO. Durable receipts prove funding, correlate Payment Links, cap refunds and reconcile missing events. Multiple valid payment/invoice/webhook receipts may fund the same canonical subscription-period but never create additional recurring bundles; receipt IDs are funding evidence, not recurring grant identity. Not a general accounting/invoicing system |
| `BillingWebhookEvent` | Retain unique provider/event ID + digest; add safe normalized replay input, processing state/attempt/error and lease fields | Recover failures after receipt; same ID/different digest is rejected. Transport dedupe is separate from the durable business-effect keys on receipts/grants |
| `IdempotencyRecord` | Extend with request fingerprint coverage, actor/reason/safe before-after references for operator mutations; preserve completed audit evidence independently of replay TTL | Reuse existing mutation identity for auditable CLI actions rather than a new admin framework. Grant/catalog/receipt evidence is immutable; expiration of response replay cannot permit a second business effect |
| `ContentGeneration` / `ContentGenerationAttempt` | Freeze funding/policy/route and dispatch identity; extend the existing attempt table with immutable dispatch-start/outcome receipt kinds, normalized nullable usage and cost evidence, completeness, timestamps and safe request ID. Preserve minimal identity when users clear/delete content | Reuse real Content execution evidence, including empty output, parse failure, cancellation and stale completions. Ledger/attempt FKs cannot be erased with content-history deletion; archive/redact payload instead |
| **New `AgentModelAttempt`** | Small actual-narration-call evidence owner linked to `AgentTaskRun`: dispatch ID/ordinal, immutable start/outcome receipts, funding/actual route, frozen pricing revision, nullable normalized usage/cost and outcome | Agent currently has no model-attempt history; `AgentToolAttempt` is only evidence-tool history. This third table is justified, not a new wallet. Successful run usage remains a display projection; do not fabricate audit rows or broaden audit `ProviderAttempt` |
| `ProviderConnection` / `ProviderRoute` / `ProviderConnectionTest` | Add a constrained `app_model` versus fixed `measurement` purpose/adapter branch; typed Content/Growth route target, user model, credential/route revision and exact-revision probe result. Composite workspace/connection ownership and one active default per workspace/feature; use one connection for both features if selected | Reuse existing encrypted key custody; do not create `CustomProvider`, a second vault or an active competing `DiscoveryModelConfig` writer. Retain strict fixed measurement routes. Existing platform-source system-workspace records must become non-secret metadata only: reject stored platform ciphertext and resolve platform secrets externally, rather than preserving the current provisioning behavior. Funding/attempts store safe references; customer-key deletion/rotation invalidates use without erasing financial references |
| Existing `UserIdentity` and account intro claim | No new verification/mail table. Launch intro policy requires no verified email; optional later work-email mode checks trusted OAuth `email_verified` against the exact current account claim email and DB domain policy | Mailbox control is not verified employment. Do not add password verification/email delivery or repurpose OAuth state tokens; an ineligible future-mode account may use only the explicitly authorized operator exception path |
| `ProviderAttempt` / `ExecutionCostProjection` | Keep existing audit evidence and reuse pure nullable/versioned cost normalization math where suitable | No audit rewrite or duplicate `UsageEvent`. Persist first frozen Content/Agent expense evidence on their native attempts; repricing/generalized expense projections are out of scope. Token observations, internal credit units and currency amounts remain distinct |

**Three** new tables are proposed: durable catalog revision, normalized payment receipt, and the genuinely missing Agent model-attempt history. All other capabilities extend current owners. If implementation proves an extra table is necessary, stop and document the invariant that cannot be met with this model before widening scope. Catalog JSON is typed/validated, not arbitrary executable policy. Stable offer IDs and account/offer transaction locks must preserve lifetime redemption limits across catalog revision publication; do not reset counts by changing revision IDs.

### Tasks
1. **[after approved-plan checkpoint] Phase 0 — Restore a trustworthy CI baseline.**
   - Use the failure audit's exact PR #35 run/head and proven failing owner list. Separate actual runtime defects from tests/fixtures that predate signup grants and new Content/Growth Agent capability enforcement.
   - Repair only demonstrated failures: entitlement-specific fixture setup must deliberately request bare/free/explicit-grant accounts; browser fixtures must return realistic resolved entitlements and onboarding state. Do not remove authorization, make inflated paid allowances the expected behavior, weaken assertions, add skips, or rewrite unrelated product code.
   - CI-proven owners: `backend/tests/component/{test_billing_api,test_billing_commercial,test_entitlement_occupancy,test_projects_prompts_api,test_prompt_generation_api,test_traffic_sync_api,test_agent_worker}.py`, `occupancy_helpers.py`, unit `test_billing.py` and `test_entitlement_contracts.py`; browser `e2e/{content,shell,design-repair}.spec.ts` and `helpers/app-fixture.ts`. Preserve pending/mismatched payment no-new-grants assertions by causal source/delta, not blindly adding three rows to expected counts.
   - Independent minimal fixes: `analysis/site_health/fact_authorship.py` / `content_heuristics.py` must reject “Powered by Vendor Example” while retaining genuine bylines (`test_site_health_parser.py`); `test_site_health_fact_regions.py` must classify extracted facts at the classifier layer; reconcile `test_site_health_page_kinds.py` with the documented FAQ question-shaped/missing-answer semantics. `test_opportunities_service.py` must compare recomputed current guidance, not its intentionally mutated stale ORM object. No broad classifier rewrite.
   - Add proven bootstrap/feature/fixture consumers to `scripts/validation.json`; align Playwright reporter/artifact paths in `frontend/playwright.config.ts` and `.github/workflows/ci.yml` so actual traces/error contexts are retained rather than uploading a nonexistent report directory.
   - Record comparison with last green main/PR run, what was reproduced and what remains pending. If an existing failure cannot be safely separated from Phase 2's billing correction, name it as an explicit dependent defect rather than claiming Phase 0 green.
   - **Gate:** exact known failures for iteration; then repository-selected mapped owners. Absent dependencies are setup blockers, not product regressions or passing tests. Keep payment network access disabled.

2. **[after 1] Phase 1 — Establish persisted commercial configuration and operator control.**
   - Update `models/{billing,content,agent,provider}.py`, `models/__init__.py` registration and `migrations/versions/0001_initial.py` for the approved minimum model, including typed financial FKs and the Agent model-attempt owner. Reuse `core/config/billing_catalog.py` types/validation while changing `domain/billing/catalog.py`, `reads.py` and quote callers to load validated persisted revisions asynchronously. Do not put DB imports/business services into `core/config`.
   - Remove commercial environment ownership from `core/config/billing_settings.py`, `.env.example` and provisioning docs; retain only secret/operational settings. Update `backend/scripts/provision_platform_provider_connections.py` and platform credential resolution to persist only non-secret platform route metadata/references, never encrypted platform keys; customer BYOK ciphertext remains in its existing scoped owner. Add model/service checks and tests preventing platform/merchant secret persistence. Move grant defaults from `core/config/entitlements.py` into DB profile templates while retaining capability definitions and algebra in code.
   - Add one thin `backend/scripts/billing_admin.py` over domain services: catalog validate/diff/publish, account inspect, explicit profile/grant/revoke, offer inspection and bounded corrections. Require an active persisted `User.role == 'admin'` identity (already used by `provision_dev_login.py`) plus trusted operator execution, actor, reason, idempotency key, dry-run and explicit target; a supplied actor UUID/email or workspace membership alone is not authority. Do not auto-promote users. Allow no raw keys on argv. Completed operator effect records are retained and immutable independently of response-replay TTL; audit output records safe references and changes, never secret-like discount codes, credentials or raw provider payloads. Validate code uniqueness, dates, rounding, nonnegative amounts, provider mode and private mappings; concurrent publication has one winner. Reuse/delegate existing `set_site_health_entitlement.py`, `provision_razorpay_plans.py`, `reconcile_billing.py`, dev-login provisioning; remove obsolete backfill assumptions rather than preserving them.
   - Seed the no-card seven-day Tier 1 campaign **draft/disabled**: Phase 1 provides configuration, not usable claim semantics. Do not enable it, assign a live cohort start, grant promotional access or invent an end date here. Its audited operator enablement follows only after Phase 4 claim/eligibility/lifetime-intro/idempotency acceptance; that enablement starts the new-registration cohort and remains independent of Razorpay and paid-checkout flags. Paid checkout stays disabled, and no live activation/deployment is authorized. Otherwise seed only approved rows from the policy table; unapproved commercial paths return structured unavailable reasons. No runtime code/environment fallback when a catalog is missing/invalid; reads do not seed or repair state. Publication invalidates catalog/entitlement caches across processes.
   - **Gate:** new catalog/operator unit/component tests plus mapped billing tests; malformed/unknown capability rejection, draft-versus-published reads, stale quote invalidation, concurrent publish, redaction, explicit target guards and seed idempotency. Assert the Phase 1 campaign is draft/disabled, makes no promotional grants and exposes no usable claim path; publishing unrelated catalog terms cannot enable it. Verify canonical recurring source-key DB uniqueness without introducing another owner. On a disposable database: clean upgrade, ORM drift check, downgrade/upgrade, exactly one migration head.

3. **[after 2] Phase 2A — Unify access resolution and transactional credit accounting.**
   - Update `domain/billing/bootstrap.py`, `domain/entitlements/{types,grants,resolver,service,enforcement,ledger,cache}.py`. Public signup is idempotent free access, not trial. Development/complimentary are explicit operator actions. Select one primary profile and add only deliberate supplemental grants; base replacement/expiry/cancellation restores the correct free fallback without stacking grants.
   - Internal state is not a provider-status shortcut: trial grants require authorization evidence; paid period grants require receipts; complimentary/development grants need audited operator authority. Time-bounded BYOK override is an explicit capability; revoke + issue implements replacement or disabling a prior override, with clear precedence. Missing/unresolved grants fail closed.
   - Fix `domain/billing/reads.py` to share one active, non-revoked per-grant aggregation with `ledger.py`: outstanding reservation is reservation minus release (do **not** subtract debit twice); active balance subtracts usage on those same active grants, not all historical debits. Historical totals remain separately labeled. Freeze reservation allocation priority through consumption, including top-up effective expiry.
   - Remove the unsafe in-process entitlement cache initially; never publish uncommitted grants, reuse identity-map lifecycle versions without fresh authority, or serve future-at results for earlier requests. GET catalog/entitlement/usage/status reads never bootstrap grants or commit repairs; account/link/profile provisioning remains on authorized writes.
   - **Lock DAG must follow callers, not invert them.** Current evidence is `prompts/locks.py` / `generation.py`: project→prompt-set→row; `entitlements/enforcement.py:11–14`: account-capacity acquired last after those locks; `audits/creation.py:154`: workspace-abuse→capacity; `audits/cancellation.py:69`: audit→task mutation→reservation release; Site Health: project→runtime→profile and runtime→membership→crawl→task (`frontier.py:375`). Before changing a lock, inventory explicit row/advisory and implicit FK/update locks for every admission, cancellation, worker, grant/revoke and provider path; document **one combined acyclic graph** in the architecture docs and prove it with real PostgreSQL interleavings.
   - The bounded extension preserves those domain-lock prefixes, then account-capacity serialization, then billing-only account/grant/receipt/intent/ledger leaves in one stable UUID/order discipline. It must **never** acquire project/prompt/runtime/audit/task locks after account/ledger locks. Pure billing mutations take capacity before billing rows and never call runtime/project projections while holding them; refresh projections in a separate committed transaction following the domain prefix. Rework current activation/grant row acquisition if it creates a back-edge. Distinguish lock acquisition order from commercial earliest-expiry allocation; lock the candidate set deterministically, then draw in frozen policy order. No provider/network work while holding any DB transaction or lock; claim/commit, perform I/O, then reacquire in the documented graph and verify authority. This is a proposed extension to prove, not a claim that today's paths already share it.
   - Recheck same-key effects after acquiring locks and serialize purchase intent/current subscription slots at the same account boundary. Reserve all required retry units atomically, earliest effective expiry then existing source/UUID ordering. Compare request fingerprints on every replay, not just key existence. Consume converts allocation release + debit in one transaction; unused reservations release once on cancellation/terminal failure/lease recovery. Refund only an existing matching debit, once within cumulative consumed units; no negative balance or cross-account/grant transfer. Never mutate history.
   - Persist the approved shared `ai_credits` policy as versioned per-model token rates, finite tier grants, grant periods and call caps. Propose integer/rational arithmetic with one ceiling at dispatch settlement, not floating-point or per-dimension repeated rounding. Normalize input/output and optional cache/reasoning dimensions without double-counting inclusive provider totals; a required missing dimension is unknown, not zero. Freeze the rate revision/rounding/cap when authorizing the call. Coefficients and quantities stay unset until explicitly supplied; BYOK routes need no credit conversion or grant.
   - Generalize reservation/consume/release/refund services to a strict audit-pair/Content-generation/Agent-run subject union; preserve small audit wrappers. A positive variable-unit settlement may debit multiple reserved grants, with per-dispatch/per-allocation uniqueness and whole-settlement fingerprint equality. Reject changed account, workspace, subject, attempt, capability or amount on replay. Retain immutable financial identity on Content history clearing and parent deletion; never cascade or null away ledger provenance.
   - Define expiration races explicitly: expiry/revocation blocks new reservations and dispatches; release unused future-call holds and refuse/replan work. Proposed policy: an already-dispatched, previously authorized call can settle its existing frozen allocation after expiry within its original customer cap; this records historical spend, never grants new credit life or enables another call. Unknown-token absorption/hold deadlines still apply. Audit's existing attempt-based timeout rule remains separate from Content/Agent's bounded unknown-token policy. Refunds retain original expiry; no outcome guesses or automatic renewal of balances.
   - Enforce at `domain/audits/{creation,resolution,funded_admission,task_creation,cancellation}.py`, `domain/providers/{service,credentials}.py`, corresponding API routes and worker attempt boundaries, plus project/prompt occupancy, Site Health runtime, Content and Growth Agent write/admission paths. A healthy BYOK credential is not plan permission; platform fallback requires separate funded eligibility and credit reservation. Capability revocation/expiry after enqueue is rechecked before external work.
   - `domain/site_health/{selection,entitlements,task_guards}.py` must count monitored URLs across all account-sponsored workspaces under the account lock and check effective validity/provenance before worker I/O, even if a positive limit did not change. Park/block excess new scheduled work deterministically after downgrade, preserving evidence. Resolve product capabilities through `GET /api/v1/workspaces/{workspace_id}/entitlements` for authorized workspace members, returning only effective capability/usage data and safe override provenance. Keep existing owner billing reads private; do not return the sponsor's payment, consent, customer or operator details to invited members.
   - Inventory `audit_cadence`, `fanout`, `exports`, and `history_window` consumers. Gate existing schedulers/fanout/export paths through the shared resolver; for unsupported enforceable benefits mark unavailable/remove the promise rather than invent a scheduler or retention subsystem. Do not delete historical evidence to implement a history limit. Preserve independent abuse, provider-capacity and crawl safety controls.
   - **Gate:** extend `test_entitlements.py`, `test_entitlement_occupancy.py`, `test_funded_ledger.py` and mapped audit/provider/content/agent tests; add regression cases for reserve3/release1/debit1 = 2 reserved, period turnover, future/rolled-back cache state, GET makes no writes, dev-email registration cannot elevate, invited-member sponsor context, two-workspace URL cap and expired unchanged-positive runtime. Use real PostgreSQL concurrent sessions for last-credit contention, cross-workspace account limits, grant/revocation races, double release/refund, payload conflicts, expiry while queued, retry timeout billing, no-grant paths and cross-account ownership. Test free→trial/paid→free, development/complimentary and override removal without paid subscriptions.

4. **[after 3] Phase 2B — Wire genuine app-model BYOK through existing custody and safe HTTP.**
   - Extend `models/provider.py`, `domain/providers/{schemas,service,credentials}.py`, `api/provider_connections.py` and existing provider DTOs with the constrained app-model branch from the owner table. Reuse encrypted `ProviderConnection`, `ProviderRoute` and `ProviderConnectionTest`; remove competing localStorage/unused-configuration writes. One connection may serve both Content/Growth targets; different explicit routes must not copy keys. Preserve fixed audit measurement validation. Support the bounded existing OpenAI-compatible chat protocol first; an arbitrary model/vendor URL does not promise support for every protocol or Growth structured output.
   - Add one small shared authenticated model-JSON HTTP boundary for Content/Agent adapters and custom probes. Reuse `connectors/web_evidence/{url_policy,resolver,targets,curl_transport}.py` low-level all-address validation, DNS pinning, TLS/peer checks and bounded transport mechanisms; **do not** pass Bearer keys through its credential-free crawler GET/redirect/canonicalization/tracing interface. Requests are bounded JSON POST, without automatic redirects, proxy-env inheritance, custom caller headers/Host, crawl policy or a general HTTP framework.
   - At create/update and every probe/dispatch/retry: strict HTTPS; reject userinfo/query/fragment secrets, malformed hosts, unapproved ports, private/loopback/link-local/metadata/reserved or mixed IPv4/IPv6 DNS results. Bind the exact normalized host/port/API base path and credential revision; connect only to a validated pinned IP with original-host TLS SNI/certificate verification. A 3xx is refusal, not key forwarding. DNS-check followed by unpinned ordinary `httpx` is insufficient. Test transports—not localhost exceptions—serve local mocks.
   - Only authorized workspace credential managers (owner/admin) may create/rotate/select/delete/probe. Preserve/strengthen fresh-key plus explicit-confirmation on destination changes; never send the prior key or platform credentials to a tenant's new URL. Probe the selected model with tiny capped content and a customer-provider-charge notice, without customer context/tools. Rate-limit probes; commit before I/O; store success only for the exact tested key/URL/model revision, invalidating it on change. Mask/sanitize every DTO, validation error, raw provider-error/key-echo, log and trace; never persist decrypted/ciphertext keys in commercial/attempt evidence.
   - Use one explicit workspace feature-route resolver for Content/Growth worker factories. Freeze connection/model/endpoint/key revision and funding before dispatch; revoked/replaced/unprobed/missing customer routes refuse, never silently select platform keys. Verify all existing shared-factory callers, including `api/prompts.py:437`: a BYOK path cannot keep an implicit platform default. Reuse an explicitly authorized app-model route or refuse unsupported routing; do not silently extend the AI-credit charge schedule to prompt generation or invent another platform-funded flow.
   - **Gate:** extend `component/test_provider_connections_api.py`, `unit/test_provider_credentials.py` and native adapter tests; add focused authenticated-model-HTTP/probe cases and strict DTO/OpenAPI checks. Prove both workers receive the selected custom model; reader/cross-workspace denial; concurrent probe/update binding; fresh-key destination rule; private/mixed/rebound DNS, redirects, TLS/host/path errors; bounded malicious output/key echo; encryption/rotation/redaction and no fallback. No real customer/provider request is needed for these tests.

5. **[after 4] Phase 2C — Meter current Content calls end to end.**
   - Owners: `domain/content/service.py`, `workers/content_worker.py`, `models/content.py`, `connectors/discovery_models/{factory,openai_compatible}.py`, Content request/response DTOs and existing task-queue/sweeper hooks. `enqueue_generation`, `regenerate` and `try_again` each perform sponsor-scoped feature/credit authorization and idempotent reservation in the job transaction. A manual new run is not a replay of the prior paid attempt. Audit BYOK does not bypass platform Content credits.
   - Use Phase 2B's verified scoped route: genuine customer-key calls create evidence but no platform-credit hold/debit; an explicitly permitted platform route requires the frozen AI-credit policy and cap. Recheck key/route revision and permission before dispatch; never substitute platform credentials after a customer-key failure.
   - Enforce actual input/context and output limits plus a real execution deadline; freeze policy and conservative customer charge cap for a platform call. Require all conversion/allowance/model bounds needed by the approved unit policy before admission. Replenish a retry hold transactionally **before** its next I/O. Worker rechecks membership-derived sponsor authority, feature/grant validity, lease/cancellation and actual provider route; configuration changes cannot silently dispatch/price the old frozen route as another provider.
   - Add durable dispatch-start and immutable outcome receipts to `ContentGenerationAttempt`, with native parent FK/call ordinal, actual route/funding, safe request ID, normalized nullable usage/completeness and frozen conversion/cost evidence. Commit the start/hold before network I/O. Capture usage before empty-output or schema validation. Persist a stale/cancelled call's usage even when lease fencing forbids publishing its content; never attach it to the successor's attempt.
   - Settle each dispatch once across as many reserved grants as needed; release the unused allocation. Proposed policy charges confirmed tokens even after empty/invalid output, while pre-I/O errors consume none. Missing required usage retains a bounded hold; after the approved deadline release it, record unknown/platform-absorbed exposure and expose operator review. Do not fabricate a debit or retroactively charge a released/absorbed hold when late evidence arrives. Record actual usage above the customer's reserved cap, but absorb/review the excess rather than creating a negative balance. Audit credit behavior remains unchanged by these token-specific rules.
   - Preserve dispatch/ledger identity through cancellation, retry exhaustion and recovery; change Content delete/clear to archive/redact user-facing payload while retaining minimal financial references under the approved retention policy. No indefinite reservation or deletion cascade. Existing provider errors must carry safe usage observations when available, not turn confirmed usage into `None`.
   - **Gate:** mapped `component/{test_content_api,test_task_queue_content,test_funded_ledger}.py`, `unit/{test_content_config,test_discovery_models_openai_compatible}.py` and focused new Content metering cases. Cover two-grant variable settlement, enqueue/replay/manual retry, deny before I/O, actual route change, zero-model misconfiguration, confirmed-token parse/empty failure, mid-call expiry/revoke/cancel, stale completion/process death, incomplete/malformed usage, bound expiry/late receipt, no over-cap debit and financial identity after clear/delete. Add customer-key success with no AI-credit debit, cross-workspace key denial, key rotation/revocation, no platform fallback, malformed URL/redirect/DNS-rebinding/private-network denial and request/log/response secret redaction. Prove Content task→capacity→billing-leaf ordering in PostgreSQL races.

6. **[after 5] Phase 2D — Meter current Growth Agent narration calls.**
   - Owners: `domain/agent/service.py`, `workers/agent_worker.py`, `models/agent.py`, `connectors/agent/{client,native_openai,factory}.py` and Agent DTOs. Reuse the typed ledger and pure usage/conversion helpers proved for Content; add `AgentModelAttempt` rather than repurposing evidence-only `AgentToolAttempt` or fabricating an audit. `submit_task`, worker claim, cancellation, retry and terminal paths preserve one real dispatch identity each.
   - Bound input/evidence size, output and execution deadline; the current lease duration alone is not execution control. Reuse the same explicit feature BYOK/platform routing contract: recheck active Tier 2/3/override plus a valid scoped customer key **or**, only for an independently permitted platform route, sufficient AI credits. Write/commit its dispatch and any required platform hold, then perform I/O without an open transaction. Never charge a genuine BYOK call or silently use the platform gateway after its failure. Preserve full normalized input/cache/output/reasoning observations supported by each adapter and unknown dimensions explicitly; cost money remains nullable and distinct from credit conversion.
   - Current tools only read persisted evidence; charge **only the optional one model narration per attempt**, with its input including that evidence. No gateway/deterministic fallback or evidence-only failure consumes model credits. A fallback product success after a failed paid narration still retains/settles that narration's confirmed usage; parse failure, cancellation and stale lease cannot erase it. Apply the same capped settlement, bounded unknown-use absorption and debit-refund policy as Content, with a fresh call identity/hold before each real retry.
   - Do not implement streaming, Agent→Content orchestration or parent/child reservation transfer. Keep a documented future invariant only: any newly supported paid tool requires bounded pre-I/O authorization and one charge per actual leaf call before it can be enabled.
   - **Gate:** mapped `component/{test_growth_agent_api,test_agent_worker,test_funded_ledger}.py`, `unit/{test_growth_agent,test_agent_worker,test_default_agent_client}.py` and new native-adapter/attempt-evidence cases. Verify genuine customer-key narration never spends AI credits, disallowed platform fallback never occurs, permitted platform narration remains metered regardless of a historical plan label, and revoked/wrong-scope/custom-endpoint credentials fail safely. Also cover confirmed-token failed parsing/fallback, no-model/no-credit consumption, deadline/input cap, cancellation/lease recovery/late receipt, retry uniqueness, expense evidence and no cross-feature double debit. Add PostgreSQL mixed Content/Agent last-credit contention against the approved shared AI pool.

7. **[after 6] Phase 3 — Make provider settlement, renewals and recovery safe.**
   - Update `connectors/billing/{base,razorpay}.py`, `domain/billing/{activations,service,webhooks,reconciliation,idempotency}.py`, `api/billing.py` and the existing reconciliation CLI. Keep all paths disabled for real purchases.
   - Bind provider plan/method/currency/amount to the frozen quote, including funded credit charge and tax/discount totals. Verify funding evidence before period grants; never issue a paid bundle from `active` status alone. For base plans and recurring add-ons, the canonical grant identity is **normalized `(internal subscription UUID, authoritative cycle start, authoritative cycle end, stable grant purpose)`**, with subscription/account ownership enforced. Normalize authoritative timestamps consistently; never derive a cycle from webhook/receipt arrival time. Encode this tuple in existing grant source keys with DB uniqueness. A stable purpose identifies a grant component within the bundle and does not vary with catalog/quote/receipt IDs. No new period table, identity service or abstraction.
   - Freeze **one complete bundle of terms per accepted subscription-period** from the purchased/approved subscription terms in existing grant/subscription provenance; apply its component grants atomically under the existing lock discipline. Payment, invoice and webhook receipts are **funding evidence only**. Two valid receipts for the same period, replay under new webhook IDs, or later catalog/quote revisions cannot produce another bundle or add changed components. Reuse the original bundle on an identical replay. Conflicting same-period terms or authoritative period boundaries refuse/quarantine for reconciliation, not new grants; a different boundary tuple alone is not proof of a new cycle. Reject overlapping/conflicting windows or attempts to reattribute known cycle evidence until authoritative reconciliation resolves them. Unknown/stale/conflicting state grants nothing.
   - Normalize Payment Link/order/payment/invoice identities explicitly; verify account/intent, expected plan, amount, currency, period and test/live mode. Normalize a captured transaction delivered under different event IDs to one canonical payment receipt. For a one-time top-up only, its grant effect key is **`(provider, canonical payment UUID, stable grant purpose)`**, with account/intent ownership enforced; two distinct purchased top-ups retain distinct payment-specific effects. Do not apply subscription-period dedupe to top-ups or payment-specific dedupe to recurring bundles. An early authenticated webhook may bind through verified opaque intent/account metadata before external-reference persistence. Quote expiry blocks a new intent, not a legitimate already-created intent's verified late payment; retain its frozen terms and resolve ambiguous abandoned intent outcomes explicitly, never silently lose a paid entitlement. Partial/failed/refunded/chargeback payments follow explicit revocation/correction rules; no automatic double refund or deletion of history.
   - Webhook receiver verifies raw-body signature and size before parsing, stores allowlisted normalized input (not raw card/customer bodies), and gives retryable events a recoverable processing state. Apply business effects and mark processed atomically; same event ID/different digest conflicts. A durable receipt cannot be lost between receipt commit and dispatch. Out-of-order event time is not sufficient proof to overwrite a newer paid period.
   - Reconciliation uses bounded persisted leases/attempts/backoff and a starvation-free cursor with `SKIP LOCKED`, commits before I/O and verifies claim ownership before applying results. Cover missed renewals, payments/refunds, failed event retry and ambiguous create/cancel/update, not only pending first activations. For an unknown create outcome, use only documented lookup/correlation by the supplied opaque intent metadata; if reliable recovery is unsupported, quarantine for operator review and keep that create path unavailable—never invent an API or create again blindly. Authentication/permission errors are not provider not-found and must not abandon a financial intent. Reuse one-shot CLI and existing scheduled-task conventions; no new daemon/queue.
   - Require bounded authoritative cycle start **and end**, mapping/amount/method/account identity and consistency with accepted subscription-period history; equal or zero provider event timestamps do not override conflicting boundaries/terms without documented authoritative reconciliation. Expose owner-authorized `GET /api/v1/billing/activations/{activation_id}` for current status instead of mutating the frozen idempotent command response; never authorize it by activation UUID alone.
   - Checkout kill switch blocks new purchases/mandates, not existing valid grants, inbound events, reconciliation or cancellation. Add bounded current/previous webhook-secret verification according to the existing rotation runbook. For an explicitly authorized future money refund, reuse the provider boundary with documented `X-Refund-Idempotency` and normalized refund receipt/cumulative refund caps; mock and keep live-disabled. Never assume that header makes subscription creation idempotent.
   - Failed invoices never grant the next credit bundle. Apply approved access/grace boundaries separately; recovery requires that specific period's paid receipt, not a generic active transition. Existing approved supplemental grants remain independently attributable.
   - **Gate:** deterministic provider-contract fixtures and `unit/{test_billing,test_billing_quotes}.py`, `component/{test_billing_api,test_billing_commercial}.py` for signature/redaction, duplicate/different-digest events, crash-after-receipt, duplicate logical invoice, missing webhook, reconciliation/webhook races, lease recovery, amount/tax/currency mismatch, wrong environment, top-up link capture, catalog revision change, refund caps and paid→past-due→recovered periods. Add explicit sequential and concurrent cases: **two valid receipts for the same subscription-period produce one frozen bundle**; duplicate webhook/receipt delivery has the same effect; **two different authoritative periods each grant once**; **two distinct purchased top-up payment receipts each grant once**; conflicting period boundaries or same-period terms quarantine with no additional grants. Verify catalog/quote/receipt-ID changes cannot alter recurring identity and cross-account evidence cannot fund the subscription. No live or unapproved sandbox calls.

8. **[after 7] Phase 4 — Add bounded commercial journeys: changes, offers and introductory access.**
   - Extend the existing billing API and strict DTOs for server quote→consent→pending intent→authoritative settlement. Client submits selected keys/quantity/country/code and consent, never prices, provider IDs, entitlement values or a trusted account ID. Quote fingerprint includes actor/account, catalog revision, mode, country/tax, offer identity, quantity and expiry; revalidate changed/stale selections before purchase.
   - Plan changes use the approved timing policy and one scheduled intent. Keep current access until the effective settled period; downgrade over-limit accounts retain data/read access but cannot add new occupancy. Stop base-dependent add-on entitlement at the correct boundary; reconcile provider cancellation of add-ons so hidden continuing charges cannot be ignored. Top-ups issue once per verified canonical provider/payment UUID plus stable purpose, with frozen expiry; distinct purchased top-up payments each issue once. Recurring base/add-on grants instead follow Phase 3's subscription-period-purpose identity and frozen bundle, not payment receipt identity.
   - Offers/codes use stable policy IDs and transactional reservations in `PendingActivation`: per-account and total limits count live reservations and successful redemptions, expiration/failure releases capacity, successful settlement consumes once, and retries/revision changes cannot reset limits. Quote flat/percentage rounding, minimum/maximum discounts, duration, eligibility and post-offer renewal amount server-side. No stacking unless separately approved.
   - Implement the claim semantics for the **draft/disabled campaign seeded in Phase 1**; keep it disabled until the Phase 4 acceptance gates below pass. The eventual **seven-day no-card Tier 1 promotion** is a primary promotional grant bundle, never a fake card-trial/provider subscription. Enforce audited enablement/cohort and any subsequently configured campaign dates, account claim/eligibility and once-only history; require no card data/authorization, never schedule a charge, never stack promotional/trial/paid base bundles, and return to baseline access when it expires. Launch policy is `new_account`: **no company/email verification gate**. After gated operator enablement, its claim is governed by the DB campaign, not Razorpay configuration or the paid-checkout switch; it must work while payments remain disabled. `domain/auth/service.py` and `oauth_service.py` record new-registration cohort eligibility only; login/billing repair never starts or regrants the offer. Implement one authenticated claim that atomically reserves/consumes the proposed shared lifetime intro slot with stable campaign identity. Seed later optional `oauth_verified_work_email` policy support using existing trusted `UserIdentity.email_verified` only for the exact current claim email plus DB domain eligibility; a domain denylist is not verified employment. Add no mail connector, verification challenge or password-email workflow. Operator codes are hashed/high-entropy, account/email-bound, expiring, rate/redemption-limited, with explicit actor/reason and named waiver scope. Proposed default waiver covers only the optional work-email restriction, not intro lifetime, tier, duration or card authorization. Broader help uses an explicit separate complimentary-grant action, never an automatic paid charge or unlogged public bypass. UI must identify promotional expiry, not a recurring renewal or mandate.
   - New-account **future card-authorized Tier 1-only** trial: reject Tier 2/3/Enterprise trial requests; reserve an account-wide trial slot, capture unbundled explicit privacy/data-sharing and recurring renewal/cancellation consent with versioned **Tier 1** terms and exact renewal quote, then send the customer to hosted card authorization. A different tier requires an explicit new selection/quote/consent; never default conversion to an upper tier or switch the funding mode invisibly. Grant nothing on browser return. Only verified card authorization tied to the account/intent and an approved future first-charge schedule starts the trial. Set `trial_started_at` at verified authorization and promise a full seven days; if the provider cannot schedule seven days after delayed authorization, keep trial unavailable rather than shortening it.
   - Store only opaque mandate/customer/subscription references, verified method/authorization status, consent versions and timestamps. Never PAN, CVV, card expiry, raw card body, browser-stored card details or invented fingerprint/tokenization APIs. Account eligibility survives workspace changes and cannot be reset by reapplying a coupon. Failed authorization issues no grants; trial cancellation/end/first-charge failure follows the explicit state policy with no fresh funded allocation.
   - Capability checks must separately establish Razorpay merchant KYC/Subscriptions activation, card/debit recurring support, international currency/method and limit eligibility, matching provider plan/offer amount, first-charge scheduling, authorization-hold disclosure, cancel/update support and legal/tax copy. Secrets and flags alone are insufficient. Razorpay `addons` are upfront/one-time; offers are documented card-only; UPI/eMandate changes have restrictions. Unsupported combinations return unavailable.
   - **Gate:** extend those four billing/quote/API owners plus auth registration/OAuth component tests for changed amounts, expired quote, unauthorized member, duplicate code/trial contention, coupon abandonment, offer duration and renewal price, failed/delayed authorization, unapproved method, once-only trial, trial→paid only on first settled invoice, cycle-end changes, quantity bounds, over-limit downgrade and add-on/base cancellation races. Add launch personal-email/no-verification acceptance, password/OAuth cohort parity, no login/repair/second-workspace regrant, no-card/card once-only races, explicit paid base replacement, campaign/code expiry and account binding; later-mode exact OAuth-email mismatch/DB domain rules; operator waiver cannot bypass lifetime/tier/card rules. Explicitly gate enablement on passing claim, new-account eligibility, shared lifetime-intro and idempotency/parallel-claim tests; verify no claim/grant while disabled and exactly one seven-day bundle after explicit claim once enabled, with Razorpay unconfigured and paid checkout off. Provider sandbox capability/consent verification remains a named pending prerequisite for payment/card-trial paths only, not for the no-card promotion.
   - **[after this phase's acceptance gates] Audited enablement:** use the existing operator CLI/domain mutation to enable the campaign only after the passing evidence is recorded, with actor/reason, explicit target, idempotency key and dry-run. Verify the enable action does not claim for anyone; the cohort begins at enablement and each eligible new account must claim explicitly. No invented campaign end date. Demonstrate the transition only on a disposable test target; this plan does not authorize live campaign activation, payment activation or deployment. Later disabling/end configuration stops new claims without changing existing seven-day grants.

9. **[after 8; UI contract work may parallel 4–8 once DTOs are frozen] Phase 5 — Reuse the existing billing/pricing UI and remove contradictory gates.**
   - Apply approved Design-tab states using existing Barlow/blue tokens, `Panel`, `Alert`, `Badge`, `UsageMeters`, dialog and input components. No redesign or admin dashboard. Owners: `frontend/components/marketing/pricing/*`, `components/settings/{billing-settings,provider-settings}.tsx`, `components/providers/{discovery-model-card,connect-provider-dialog}.tsx`, `lib/providers/use-engine-connection.ts`, `lib/api/{providers,billing}.ts` and their matching `lib/api/schemas/*` DTOs, `components/billing/*`, `lib/billing/{catalog,entitlement-context,pending-pricing-intent}.ts*`, `lib/api/billing.ts`, `lib/api/schemas/billing.ts`, `lib/config/billing.ts` and existing Content/Agent gating call sites.
   - Delete the hardcoded plan-price map and false fallback amounts. Marketing, Settings, comparisons, quotes and confirmation use the same server catalog and approved USD values; never relabel unchanged USD numbers with an INR symbol. Separate workspace-effective entitlement queries/cache keys for product controls from owner-private billing account queries; refresh on workspace switching. Use capability-type-aware checks (a level of `unset` is not true), effective dates and grant authority instead of historical-plan-grant heuristics. Keep loading/error/unpriced/unsupported distinct. Checkout-disabled does not hide valid complimentary/development/BYOK access; plan or key names never authorize UI.
   - Show actual free/no-card-promotion/card-trial/development/complimentary/paid state separately from pending/past-due/cancelled provider status. Display effective tier/grant provenance, BYOK override source/expiry, finite/unknown usage, reserved/consumed/refunded/available credits, expiry, renewal/first-charge dates and amounts, cancellation/over-limit state. Do not label every subscription Active.
   - Minimally extend the existing provider Settings UI to select Content/Agent model, custom API key and base URL using the audited existing API and safe masked-secret controls; show actual credential source, per-feature routing/validation status, customer-paid costs versus platform AI credits, permission failures and no-fallback behavior. Do not invent a second credential/settings page. Add a clearly separate no-card **any-new-account** promotional claim state, with optional operator-code entry and expiry-to-free language—not card renewal or a launch email gate. The later OAuth-work-email policy may show eligibility/exception instructions only when that DB mode is enabled; never claim verified employment.
   - Reuse a bounded quote/discount/card-trial dialog: show exact server charge, discount duration, later renewal price, trial boundaries, card-only hosted handoff, privacy/provider-sharing/renewal/cancellation disclosures and unchecked explicit consent controls. Payment-disabled state has no operative checkout/card capture. Anonymous intent capture stores selection only; authenticated resume re-quotes and never auto-consents or automatically repeats a mutation.
   - **Gate:** update existing pricing, billing Settings, API/schema and catalog tests; add `frontend/e2e/billing.spec.ts` and map it plus relevant marketing/provider/shell scenarios in `scripts/validation.json`. Real browser checks cover mobile/desktop, keyboard/focus/error states, same-origin `/api/*`, fresh account/free access, explicit development/complimentary override, revoked BYOK, unavailable checkout, trial consent and stale offers. Verify CiteLadder never collects raw card fields/bodies. Customer BYOK keys may appear only in the deliberate masked write-only input and its authorized HTTPS save request; they must not be echoed in responses, persisted in browser storage, exposed after save, or appear in logs/traces/analytics/provider-error text. Test secret rotation and malicious key-echo responses explicitly.

10. **[after 1–9] Phase 6 — Review, simplify and close the full validation gate.**
   - Review every added owner and abstraction against the minimum table: remove duplicate balances/catalog defaults, stale commercial env variables, provider-status permission shortcuts and orphan test/fixture paths. Preserve unrelated PR #35 work; refactor complexity instead of raising ceilings or adding exceptions.
   - Update shipped ownership in `docs/backend-architecture.md`, `docs/frontend-architecture.md`, `docs/invariants.md`, `docs/DEVELOPMENT.md` and `docs/operations/razorpay-and-demo-owner-requirements.md`; correct its unsupported “verify checks provider amounts” claim, stale env commercial ownership and event checklist without expanding or restoring the unrelated demo funnel. Update contract/error docs and validation mappings. Distinguish implemented disabled flows from externally verified availability, and archive only directly superseded billing guidance.
   - **Operational guide deliverable:** ship `docs/operations/billing-operator-guide.md`, linked from the plan/runbook, with exact commands copied from the implemented CLI and exercised against deterministic disposable fixtures—not placeholders or proposed syntax. Cover catalog export/edit/validate/diff/publish and select-previous-revision; offer create/edit/date/scope/total/per-account limits; grant/revoke/BYOK override/credit correction/expiry; account/ledger/receipt/intent inspection; bounded reconcile/status/dry-run; draft-campaign inspection and audited enablement only after Phase 4 acceptance, independent of paid checkout; trusted operator authentication and first-admin bootstrap without email privilege or password/secret argv; current/previous secret rotation; payment-disabled operation and incident switches; and the rollback procedure below. Include expected safe output, idempotency/replay behavior, required authority, irreversible effects and a dry-run before every mutating example. Existing `provision_dev_login.py` remains development-only, not a production admin bootstrap bypass.
   - **Rollback, not history rewriting:** stop new checkout and affected new metered-work admission with operational switches while preserving reads, receipt ingestion/reconciliation, settling already-dispatched work and cancellation. Restore/select a prior immutable validated catalog revision for **future purchases only**; already accepted quotes/intents/subscriptions/grants retain their frozen terms unless explicitly cancelled/corrected under their rules. Compensate mistaken grants/credits through append-only revocation/issue/refund records with actor/reason, never delete receipts or edit ledger history. Reverting code requires a compatible schema; disposable databases alone may be reset/rebuilt with matching code + the sole initial migration. This does not authorize or imply any live/production downgrade, reset, deployment or cash refund. Test rollback/republication does not duplicate renewals, resurrect expired credit, lose pending settlements or re-open consumed offer/trial claims.
   - Repeat disposable clean reset/migrate/ORM-drift verification for the final schema, inspect UUID/FK/unique/check constraints, and review redaction/least-privilege/account isolation. No live reset, deployment or provider activation.
   - Finish one full local static run followed by the repository-selected mapped tests in the required order, then obtain the full authoritative GitHub CI owners on the final implementation head as described below. Record exact SHAs/results and remaining external checks; do not declare fully validated while an owner is skipped or prerequisites are missing.

### Testing
**Current evidence versus future checks:** audit inspection and narrow reproductions do not constitute a passed implementation suite. Backend venv, frontend dependencies, `uv`, `pnpm` and PowerShell were absent during the initial audit; setup issues were reported (failure audit #719; coordinating agent #720). No setup or dependency installation is part of this approval-only work. Install the pinned prerequisites before implementation validation (`uv sync --frozen --extra dev`, frontend `pnpm install --frozen-lockfile`, matching Node/Python, PostgreSQL/test URL, PowerShell and Playwright browser dependencies). Tests never read `.env` or inherit provider keys. Use a disposable test database. Never run the full backend suite locally.

At each completed bounded phase, use the validation mapping to run its affected owners; exact known-failure tests are allowed while debugging. Add missing production/E2E mappings to `scripts/validation.json`, never use a broad fallback or weaken selectors. Do not repeatedly run full release gates after each edit. Before declaring completion or pushing implementation, run once in order from repository root:

```powershell
.\scripts\check.ps1
.\scripts\test.ps1
```

The first test-selector run uses the full working diff against `origin/main`. After a failed selector run, a retry may use `-ChangedFiles` with **every** subsequently edited file, never manually selected test files. Review/include formatter changes. `check.ps1 -CheckOnly` is the nonmutating verification variant; a missing `pwsh` command is pending setup, not permission to omit required gates.

| Gate | Required evidence |
|---|---|
| Static and contract | Ruff/mypy/backend architecture/complexity/dead-code, Oxlint/format/TypeScript/frontend policies, strict OpenAPI/schema and docs checks via the repository owner |
| Database rebuild | Only `0001_initial.py`; disposable reset, `alembic upgrade head`, `alembic check`, `alembic downgrade base`, upgrade again; zero ORM drift and idempotent approved seeds |
| Accounting/security | Real PostgreSQL contention and rollback tests; funding/occupancy conservation; canonical recurring subscription-period-purpose uniqueness versus payment-specific top-up uniqueness, duplicate receipts and conflicting-period quarantine; refund and expiry limits; replay fingerprint conflicts; owner/member/workspace isolation; card/secret redaction |
| Browser | Mapped marketing/billing/provider/access E2E and approved design states, with both disabled payments and simulated authorized lifecycle; actual browser same-origin/keyboard/responsive verification |
| GitHub CI | Full backend unit/component, frontend tests/coverage/build, E2E, contract/static/security and clean-clone Compose owners on the implementation SHA, not merely aggregate green with skipped owners |
| Provider/legal | Actual merchant test-mode capability and consent/first-charge verification only after explicit access/authorization. Pending until performed; successful mocks do not establish real recurring-card or international eligibility |

Current `ci.yml` PR pushes classify only latest-push and previously failed owners; main/merge-group run all owners. Do not open a new PR or merge/deploy just to get full CI. If final PR classification skips required full owners, use a reviewed non-deploying full-validation trigger for the same PR SHA (prefer a small manual `workflow_dispatch` full-owner mode in the existing classifier/workflows, tested with `scripts/ci-changes.test.mjs`, retaining normal PR behavior). This trigger must run all actual suites, not simply force `CI / Required` green. If that narrow CI trigger is not approved, report full-CI verification pending until the existing full main/merge-group route is explicitly authorized; no full local backend substitution.

### Provider documentation and design handoff
Research: `../audits/billing-consolidation-2026-09-07.md#razorpay-documentation-research`, checked 2026-09-07, with merchant capability still unverified. Official references: [subscription creation/trial](https://razorpay.com/docs/payments/subscriptions/create/), [states](https://razorpay.com/docs/payments/subscriptions/states/), [payment retries](https://razorpay.com/docs/payments/subscriptions/payment-retries/), [offers](https://razorpay.com/docs/payments/subscriptions/offers/create/), [subscription changes](https://razorpay.com/docs/api/payments/subscriptions/update-subscription/), [webhook validation](https://razorpay.com/docs/webhooks/validate-test/) and [best practices](https://razorpay.com/docs/webhooks/best-practices/). Verify applicability for the actual merchant and region before enabling anything; do not claim generic card/currency support from mixed regional documentation.

The final design manifest is `billing-consolidation-designs/design-plan.json`. Its five separate artifacts are:
- `billing-consolidation-designs/billing-marketing-pricing-payment-disabled.html`
- `billing-consolidation-designs/billing-settings-account-usage-overrides.html`
- `billing-consolidation-designs/billing-no-card-early-access-offer.html`
- `billing-consolidation-designs/billing-trial-discount-quote-consent.html`
- `billing-consolidation-designs/billing-byok-provider-configuration.html`

 The main agent must attach the manifest and separate artifacts to the Design tab and preserve them with the approved checkpoint. Design illustration values never become seed policy; known funded prices are “priced but unavailable,” not “not yet priced,” and tier names remain unchanged. The unchanged mockups depict intended final states, not Phase 1 campaign availability. Static proposed states are not an implemented or live payment preview. Refund API references: [normal refund idempotency](https://razorpay.com/docs/api/refunds/normal-refunds-idempotent/?preferred-country=IN) and [instant refund idempotency](https://razorpay.com/docs/api/refunds/instant-refunds-idempotent/?preferred-country=IN).
